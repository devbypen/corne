/*
 * Corne (CRKBD) ZMK configuration - 5 column (3x5+3, 36 keys)
 *
 * The stock ZMK corne shield already defines the kscan, OLED and both the
 * 6 and 5 column layouts; here we only pick the 5 column layout and add RGB.
 */

#include <dt-bindings/led/led.h>

/ {
	chosen {
		zmk,physical-layout = &foostan_corne_5col_layout;
		zmk,underglow = &led_strip;
	};
};

/* RGB Underglow - SPI */
&pinctrl {
	spi3_default: spi3_default {
		group1 {
			psels = <NRF_PSEL(SPIM_MOSI, 0, 6)>;
		};
	};

	spi3_sleep: spi3_sleep {
		group1 {
			psels = <NRF_PSEL(SPIM_MOSI, 0, 6)>;
			low-power-enable;
		};
	};
};

&spi3 {
	compatible = "nordic,nrf-spim";
	status = "okay";

	pinctrl-0 = <&spi3_default>;
	pinctrl-1 = <&spi3_sleep>;
	pinctrl-names = "default", "sleep";

	led_strip: ws2812@0 {
		compatible = "worldsemi,ws2812-spi";

		/* SPI Configuration */
		reg = <0>;
		spi-max-frequency = <4000000>;

		/* 27 LEDs per half (each half drives its own strip) */
		chain-length = <27>;
		spi-one-frame = <0x70>;
		spi-zero-frame = <0x40>;
		color-mapping = <LED_COLOR_ID_GREEN
				 LED_COLOR_ID_RED
				 LED_COLOR_ID_BLUE>;
	};
};
